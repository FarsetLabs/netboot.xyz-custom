#!ipxe
###
### netboot.xyz-custom menu example
###

:custom
clear custom_choice
menu Windows Menu
item --gap Windows menu
item option_one ${space} Boot Windows 7
item option_two ${space} Test
choose custom_choice || goto custom_exit
echo ${cls}
goto ${custom_choice}
goto custom_exit

:option_one
chain tftp://${custom_root_domain}/windows/win7.ipxe

:option_two
chain tftp://${custom_root_domain}/windows/test.ipxe

boot || goto custom_exit